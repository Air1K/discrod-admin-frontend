import { AppRouterProvider } from './router'
import { ThemeProvider } from './theme'
import { ReactQuery } from './reactQuery'

export const Providers = {
  router: AppRouterProvider,
  theme: ThemeProvider,
  rectQuery: ReactQuery,
}
