import { type FC } from 'react'
import { Button } from '@/shared/ui/shadcn/Button'

const LoginFromDiscordButton: FC = () => {
  return <Button className={'w-full'}>Войти через Discord</Button>
}

export { LoginFromDiscordButton }
